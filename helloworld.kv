#:import Button kivy.uix.button

Button:
	text: "Hello World"
	size_hint: (0, 0)
	size: (700, 500)
	pos: (50, 50)
	background_color: (0, 0, 255, 0.3)
	on_press: app.play_music()
	on_release: self.text = "Playing: " + app.sound.source.split('.')[0]